# ✅ 問題已解決！PaddleOCR 服務成功啟動

## 🔍 問題診斷過程

### 問題 1: 檔案路徑錯誤

```
ERROR: "/services/paddleocr/paddleocr_service.py": not found
```

**解決：** 修正 Dockerfile 中的 COPY 路徑為 `start_paddleocr_service.py`

### 問題 2: 套件版本不相容

```
ERROR: Could not find a version that satisfies the requirement paddlepaddle==2.5.1
```

**解決：** 更新為最新版本 `paddlepaddle==3.2.2`

### 問題 3: 缺少系統函式庫

```
ImportError: libGL.so.1: cannot open shared object file
```

**解決：** 在 Dockerfile 中添加 `libgl1` 套件

## ✨ 目前狀態

### 🟢 PaddleOCR 服務

- **狀態：** ✅ 正常運行
- **容器：** `paddleocr-container`
- **端口：** `8866`
- **健康檢查：** `http://localhost:8866/health` → `{"status":"healthy"}`

### 🟢 Spring Boot 應用

- **狀態：** ✅ 正常運行（背景任務）
- **端口：** `8080`

## 🚀 使用方式

### 1. 測試 PaddleOCR 服務

```bash
./test_paddleocr.sh
```

### 2. 上傳圖片進行識別

```bash
curl -X POST http://localhost:8080/api/upload \
  -F "file=@your-image.jpg" \
  | python3 -m json.tool
```

### 3. 查看服務日誌

```bash
# PaddleOCR 日誌
docker logs -f paddleocr-container

# Spring Boot 日誌
# (在 VS Code Terminal 中查看)
```

## 📊 處理流程

```
圖片上傳 (your-image.jpg)
    ↓
Spring Boot Controller (port 8080)
    ↓
OcrService.extractTextFromImage()
    ↓
PaddleOCR Service (port 8866) → 提取文字
    ↓
提取的文字內容
    ↓
PromptBuilder.build(extractedText)
    ↓
AiExtractService.extract()
    ↓
Llama Model (port 11434) → 分析並結構化
    ↓
返回 JSON (CompanyInfo)
```

## 🛠️ 管理指令

### 容器管理

```bash
# 查看運行狀態
docker ps | grep paddleocr

# 停止服務
docker stop paddleocr-container

# 啟動服務
docker start paddleocr-container

# 重啟服務
docker restart paddleocr-container

# 移除容器
docker rm -f paddleocr-container
```

### 使用 docker-compose

```bash
# 啟動
docker-compose up -d

# 停止
docker-compose down

# 查看日誌
docker-compose logs -f
```

### 使用 Makefile

```bash
make start    # 啟動
make stop     # 停止
make restart  # 重啟
make logs     # 查看日誌
make test     # 測試服務
make clean    # 完全清理
```

## 📝 注意事項

1. **第一次啟動較慢**：PaddleOCR 會自動下載模型檔案（約 50-80 MB）
2. **記憶體需求**：建議給 Docker 分配至少 2GB RAM
3. **持久化模型**：模型檔案會儲存在容器的 `/root/.paddlex/` 目錄中

## 🎯 下一步

現在你可以：

1. ✅ 上傳圖片測試完整的 OCR + LLM 流程
2. ✅ 調整 PaddleOCR 的參數（在 `start_paddleocr_service.py` 中）
3. ✅ 監控服務性能和日誌
4. ✅ 使用 docker-compose 管理所有服務

## 🐛 如果遇到問題

```bash
# 完整重置（慎用）
docker stop paddleocr-container
docker rm paddleocr-container
docker rmi paddleocr-service
docker build --no-cache -f Dockerfile.paddleocr -t paddleocr-service .
docker run -d --name paddleocr-container -p 8866:8866 paddleocr-service

# 等待服務啟動（約 30-60 秒）
sleep 30 && curl http://localhost:8866/health
```

---

**建構時間：** 約 111 秒（不使用快取）  
**映像檔大小：** 約 1.3 GB  
**首次啟動時間：** 約 30-60 秒（下載模型）  
**後續啟動時間：** 約 5-10 秒
